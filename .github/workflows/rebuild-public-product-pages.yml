name: Archived V4 public product page generator

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived. Product pages must be changed in source, not by a workflow that commits back to the branch."
