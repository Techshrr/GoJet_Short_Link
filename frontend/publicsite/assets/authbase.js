const $=s=>document.querySelector(s);
const page=document.body.dataset.authPage||'';
const qs=new URLSearchParams(location.search);
let botPolicyPromise=null,turnstileScriptPromise=null,turnstileWidget=null;
function showMessage(text,type='error'){const el=$('#message');if(!el)return;el.className=`message${text?' show':''} ${type}`;el.textContent=text}
function setBusy(form,busy,label='处理中…'){const btn=form?.querySelector('button[type="submit"]');if(btn){btn.disabled=busy;btn.dataset.label||=btn.textContent;btn.textContent=busy?label:btn.dataset.label}}
function fallbackMessage(status){if(status===429)return'操作过于频繁，请稍后再试。';if(status===401||status===403)return'验证信息不正确，请重新检查。';if(status>=500)return'服务暂时不可用，请稍后再试。';return'当前操作未能完成，请检查填写内容后重试。'}
async function request(path,options={}){const response=await fetch(path,{...options,headers:{'Content-Type':'application/json',...(options.headers||{})}});const data=await response.json().catch(()=>({}));if(!response.ok)throw Object.assign(new Error(data.error||fallbackMessage(response.status)),{data,status:response.status});return data}
function validLocalPath(target){return typeof target==='string'&&target.startsWith('/')&&!target.startsWith('//')&&!target.includes('\\')&&!/[\u0000-\u001f\u007f]/.test(target)}
function safeRedirect(){const queryTarget=qs.get('redirect'),stored=localStorage.getItem('gojet_post_auth_path');if(validLocalPath(queryTarget))return queryTarget;if(validLocalPath(stored)){localStorage.removeItem('gojet_post_auth_path');return stored}return'/app/dashboard'}
async function botPolicy(){if(!botPolicyPromise)botPolicyPromise=request('/api/public/turnstile').catch(()=>({enabled:false,surfaces:{}}));return botPolicyPromise}
function surfaceForPage(){return page==='register'?'registration':page==='login'?'login':page==='forgot'?'forgot_password':page==='reset'?'reset_password':''}
function actionForPage(){return page==='register'?'register':page==='login'?'login':page==='forgot'?'forgot_password':page==='reset'?'reset_password':''}
function loadTurnstile(){if(window.turnstile)return Promise.resolve();if(turnstileScriptPromise)return turnstileScriptPromise;turnstileScriptPromise=new Promise((resolve,reject)=>{const s=document.createElement('script');s.src='https://challenges.cloudflare.com/turnstile/v0/api.js?render=explicit';s.async=true;s.defer=true;s.onload=resolve;s.onerror=()=>reject(new Error('人机验证组件加载失败，请刷新页面重试。'));document.head.appendChild(s)});return turnstileScriptPromise}
async function prepareTurnstile(form){const surface=surfaceForPage(),policy=await botPolicy();if(!surface||!policy.enabled||!policy.surfaces?.[surface])return null;if(!policy.site_key)throw new Error('人机验证已启用，但站点密钥尚未配置。');await loadTurnstile();let slot=form.querySelector('[data-turnstile-slot]');if(!slot){slot=document.createElement('div');slot.dataset.turnstileSlot='1';slot.className='auth-turnstile';form.insertBefore(slot,form.querySelector('button[type="submit"]'))}if(turnstileWidget===null)turnstileWidget=window.turnstile.render(slot,{sitekey:policy.site_key,action:actionForPage(),theme:'auto'});return turnstileWidget}
async function getTurnstileToken(form){const id=await prepareTurnstile(form);if(id===null)return'';const token=window.turnstile.getResponse(id);if(!token)throw new Error('请先完成人机验证。');return token}
function resetTurnstile(){if(turnstileWidget!==null&&window.turnstile)window.turnstile.reset(turnstileWidget)}
async function attachTurnstile(){const form=$('form');if(!form)return;try{await prepareTurnstile(form)}catch(err){showMessage(err.message)}}
function socialErrorText(code){return({cancelled:'第三方登录已取消。',provider_failed:'第三方登录暂时失败，请重新发起。',provider_profile:'第三方账户资料读取失败，请重新发起。',binding_required:'该第三方账户未提供可用于安全注册的已验证邮箱。请先使用邮箱注册或登录 GoJet，再到账户设置中绑定此登录方式。',email_exists:'该邮箱已经存在 GoJet 账户。请先使用原登录方式进入账户，再绑定第三方身份。',registration_closed:'当前暂未开放新用户注册。',email_blocked:'该第三方账户的邮箱域名不允许注册。'})[code]||'第三方登录未能完成，请重新发起。'}
function socialStartHref(provider,redirect,type=''){const params=new URLSearchParams({redirect});if(type)params.set('type',type);if(page==='register')params.set('flow','register');return`/api/public/auth/${encodeURIComponent(provider)}/start?${params.toString()}`}
function renderSocialProvider(provider,redirect){const types=Array.isArray(provider.login_types)?provider.login_types.filter(item=>item&&item.id&&item.label):[];if(provider.id==='rainbow'&&types.length===1){const link=document.createElement('a');link.className='social-provider';link.dataset.provider=provider.id;link.href=socialStartHref(provider.id,redirect,types[0].id);link.textContent=`继续使用 ${provider.label}`;return link}if(provider.id==='rainbow'&&types.length>1){const details=document.createElement('details');details.className='social-provider-group';const summary=document.createElement('summary');summary.textContent=`继续使用 ${provider.label}`;details.append(summary);const choices=document.createElement('div');choices.className='social-provider-choices';types.forEach(item=>{const link=document.createElement('a');link.className='social-provider social-provider-sub';link.href=socialStartHref(provider.id,redirect,item.id);link.textContent=item.label;choices.append(link)});details.append(choices);return details}const link=document.createElement('a');link.className='social-provider';link.dataset.provider=provider.id;link.href=socialStartHref(provider.id,redirect);link.textContent=`继续使用 ${provider.label}`;return link}
async function loadSocialProviders(){const root=$('#socialAuth'),list=$('#socialProviders');if(!root||!list)return;try{const data=await request('/api/public/auth/providers');const providers=Array.isArray(data.providers)?data.providers:[];if(!providers.length)return;const redirect=safeRedirect();list.replaceChildren(...providers.map(provider=>renderSocialProvider(provider,redirect)));root.classList.remove('hidden')}catch(_){}}
async function consumeSocialFragment(){const fragment=new URLSearchParams(location.hash.replace(/^#/,'')),handoff=fragment.get('social_handoff'),error=fragment.get('social_error');if(!handoff&&!error)return false;history.replaceState(null,'',location.pathname+location.search);if(error){showMessage(socialErrorText(error),'info');return false}showMessage('正在完成第三方登录…','info');try{const result=await request('/api/public/auth/handoff',{method:'POST',body:JSON.stringify({code:handoff})});localStorage.setItem('gojet_token',result.token);location.replace(validLocalPath(result.redirect)?result.redirect:safeRedirect());return true}catch(err){showMessage(err.message);return false}}
function captureSocialRegistration(){if(page!=='register')return'';const fragment=new URLSearchParams(location.hash.replace(/^#/,'')),code=String(fragment.get('social_registration')||'').trim();if(!code)return'';history.replaceState(null,'',location.pathname+location.search);return code}
const socialRegistrationCode=captureSocialRegistration();if(socialRegistrationCode)window.gojetSocialRegistrationCode=socialRegistrationCode;

if(page==='login'){
  (async()=>{const handling=await consumeSocialFragment();if(handling)return;if(localStorage.getItem('gojet_token')){location.replace(safeRedirect());return}loadSocialProviders();attachTurnstile()})();
  $('#loginForm')?.addEventListener('submit',async e=>{e.preventDefault();setBusy(e.currentTarget,true,'正在登录…');showMessage('', 'info');try{const body=Object.fromEntries(new FormData(e.currentTarget));body.turnstile_token=await getTurnstileToken(e.currentTarget);const result=await request('/api/auth/login',{method:'POST',body:JSON.stringify(body)});localStorage.setItem('gojet_token',result.token);location.replace(safeRedirect())}catch(err){if(err.data?.email_verification_required)showMessage('该账户尚未完成邮箱验证，请先完成邮箱验证。','info');else showMessage(err.message);resetTurnstile()}finally{setBusy(e.currentTarget,false)}})
}

if(page==='register'){
  if(socialRegistrationCode){$('#socialAuth')?.classList.add('hidden');attachTurnstile()}else{loadSocialProviders();attachTurnstile()}
  fetch('/api/public/settings').then(response=>response.ok?response.json():null).then(settings=>{const enabled=settings?.registration?.enabled??settings?.['registration.enabled'];if(enabled===false){$('#registerForm')?.remove();$('#socialAuth')?.remove();showMessage('当前暂未开放新用户注册。','info')}}).catch(()=>{});
  if(!socialRegistrationCode)$('#registerForm')?.addEventListener('submit',async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget));if(body.password!==body.password_confirmation){showMessage('两次输入的密码不一致');return}delete body.password_confirmation;setBusy(e.currentTarget,true,'正在创建…');try{body.turnstile_token=await getTurnstileToken(e.currentTarget);const result=await request('/api/auth/register',{method:'POST',body:JSON.stringify(body)});if(result.verification_required){e.currentTarget.reset();resetTurnstile();showMessage(result.verification_queued?'账户已创建，验证邮件已经发送。完成邮箱验证后即可登录。':'账户已创建，但验证邮件暂时无法发送，请稍后重试。','success')}else{localStorage.setItem('gojet_token',result.token);location.replace(safeRedirect())}}catch(err){showMessage(err.message);resetTurnstile()}finally{setBusy(e.currentTarget,false)}})
}

if(page==='forgot'){
  attachTurnstile();
  $('#forgotForm')?.addEventListener('submit',async e=>{e.preventDefault();const form=e.currentTarget;setBusy(form,true);try{const body=Object.fromEntries(new FormData(form));body.turnstile_token=await getTurnstileToken(form);await request('/api/auth/forgotpassword',{method:'POST',body:JSON.stringify(body)});form.reset();resetTurnstile();showMessage('如果该邮箱对应有效账户，我们已经发送密码重置链接。','success')}catch(err){showMessage(err.message);resetTurnstile()}finally{setBusy(form,false)}})
}

if(page==='reset'){
  const token=qs.get('token')||'';if(!token)showMessage('这个密码重置链接无效或不完整，请重新发起找回密码。');
  attachTurnstile();
  $('#resetForm')?.addEventListener('submit',async e=>{e.preventDefault();const form=e.currentTarget;const body=Object.fromEntries(new FormData(form));if(body.password!==body.password_confirmation){showMessage('两次输入的密码不一致');return}delete body.password_confirmation;body.token=token;setBusy(form,true);try{body.turnstile_token=await getTurnstileToken(form);await request('/api/auth/resetpassword',{method:'POST',body:JSON.stringify(body)});localStorage.removeItem('gojet_token');form.reset();showMessage('密码已更新。其他已登录设备会失效，现在可以使用新密码登录。','success');setTimeout(()=>location.replace('/login'),1200)}catch(err){showMessage(err.message);resetTurnstile()}finally{setBusy(form,false)}})
}

if(page==='verify'){
  const token=qs.get('token')||'';const action=$('#verifyAction');if(!token){showMessage('这个邮箱验证链接无效或不完整。');if(action)action.disabled=true}else{action?.addEventListener('click',async()=>{action.disabled=true;try{await request('/api/auth/verifyemail',{method:'POST',body:JSON.stringify({token})});showMessage('邮箱验证成功，现在可以登录 GoJet。','success');action.textContent='验证成功';$('#loginLink')?.classList.remove('hidden')}catch(err){showMessage(err.message);action.disabled=false}})}
}