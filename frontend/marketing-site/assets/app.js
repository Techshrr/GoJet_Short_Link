const trigger=document.querySelector('[data-mega]'),mega=document.querySelector('.mega');if(trigger&&mega){trigger.onclick=e=>{e.stopPropagation();mega.classList.toggle('open')};document.addEventListener('click',()=>mega.classList.remove('open'))}document.querySelector('.mobileMenu')?.addEventListener('click',()=>document.querySelector('.mega')?.classList.toggle('open'));
fetch('/api/public/settings').then(r=>r.ok?r.json():null).then(settings=>{if(!settings)return;const name=settings['site.name']||'GoJet';document.querySelectorAll('.logo').forEach(logo=>{if(settings['brand.logo_url']){const img=document.createElement('img');img.src=settings['brand.logo_url'];img.alt=name;img.style.cssText='max-width:150px;max-height:38px';logo.replaceChildren(img)}else if(logo.textContent.trim().startsWith('GoJet')){logo.childNodes[0].textContent=name}});if(settings['brand.primary_color'])document.documentElement.style.setProperty('--blue',settings['brand.primary_color']);if(settings['seo.default_title'])document.title=settings['seo.default_title'];const description=settings['seo.meta_description'];if(description){let meta=document.querySelector('meta[name=description]');if(!meta){meta=document.createElement('meta');meta.name='description';document.head.append(meta)}meta.content=description}if(settings['brand.favicon_url']){let icon=document.querySelector('link[rel=icon]');if(!icon){icon=document.createElement('link');icon.rel='icon';document.head.append(icon)}icon.href=settings['brand.favicon_url']}}).catch(()=>{});
