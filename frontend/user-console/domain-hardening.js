(()=>{
const DQ=s=>document.querySelector(s),DE=v=>escapeHTML(v??'');let domainsCache=[];
function dnsCard(record){if(!record)return'';return `<div class="dnsRecordCard"><div><small>TYPE</small><b>${DE(record.type)}</b></div><div><small>NAME / HOST</small><code>${DE(record.name)}</code><button onclick="gojetCopy('${String(record.name).replace(/'/g,"\\'")}',this)">复制</button></div><div><small>VALUE</small><code>${DE(record.value)}</code><button onclick="gojetCopy('${String(record.value).replace(/'/g,"\\'")}',this)">复制</button></div><p>把这条 TXT 记录添加到 DNS 后，等待解析生效，再点击“立即验证”。重新生成后旧记录立即失效。</p></div>`}
function status(v){return v==='active'?'<span class="domainDot ok"></span>正常':v==='pending'?'<span class="domainDot pending"></span>等待':'<span class="domainDot bad"></span>异常'}
async function render(){const c=DQ('.content');c.innerHTML=`<div class="productPageHead"><div><span class="eyebrow">CUSTOM DOMAINS</span><h1>自定义域名</h1><p>完成 TXT 所有权验证和 HTTPS 检查后，域名才能出现在短链接创建器中。</p></div><button class="primary productPrimary" onclick="openDomainCreator()">添加域名</button></div><div id="domainCreatePanel"></div><div id="domainHardeningList" class="productLoading">正在读取域名…</div>`;await load()}
async function load(){try{domainsCache=(await api(`/api/workspaces/${state.workspace}/domains`)).data||[];DQ('#domainHardeningList').innerHTML=domainsCache.length?`<div class="domainStack">${domainsCache.map(x=>{const id=Number(x.ID??x.id),host=x.Hostname??x.hostname,s=x.Status??x.status,h=x.HTTPSStatus??x.https_status,err=x.last_error??x.LastError??'';return `<article class="domainCard"><div class="domainHost"><div class="domainIcon">⌁</div><div><h2>${DE(host)}</h2><div class="domainStates"><span>DNS ${status(s)}</span><span>HTTPS ${status(h)}</span></div></div></div><div class="domainInfo"><div><small>最后检查</small><b>${x.last_checked_at??x.LastCheckedAt?date(x.last_checked_at??x.LastCheckedAt):'尚未检查'}</b></div><div><small>可用于短链</small><b>${s==='active'&&h==='active'?'是':'否'}</b></div></div>${err?`<div class="domainError">${DE(err)}</div>`:''}<div class="resourceActions"><button onclick="verifyDomainHardening(${id})">立即验证</button>${s!=='active'||h!=='active'?`<button onclick="rotateDomainHardening(${id})">重新生成 TXT</button>`:''}${s==='active'&&h==='active'?`<button onclick="gojetCopy('${String('https://'+host).replace(/'/g,"\\'")}',this)">复制域名</button>`:''}</div></article>`}).join('')}</div>`:`<div class="productEmpty"><div class="emptyIcon">⌁</div><h3>还没有自定义域名</h3><p>添加后 GoJet 会给你一条 TXT 验证记录，完成 DNS 和 HTTPS 检查后即可用于短链接。</p><button class="primary" onclick="openDomainCreator()">添加域名</button></div>`}catch(e){DQ('#domainHardeningList').innerHTML=`<div class="productError">${DE(e.message)}</div>`}}
window.openDomainCreator=()=>{DQ('#domainCreatePanel').innerHTML=`<section class="domainCreator"><div class="composerTop"><div><span class="eyebrow">ADD DOMAIN</span><h2>添加自定义域名</h2></div><button onclick="document.querySelector('#domainCreatePanel').innerHTML=''">关闭</button></div><form id="domainCreatorForm"><label>域名<input name="hostname" placeholder="go.example.com" autocomplete="off" required></label><p>不要填写 https://、路径或 IP。建议使用专门的短链子域名。</p><div id="domainCreatorError"></div><button class="primary productPrimary">生成 DNS 验证记录</button></form></section>`;DQ('#domainCreatorForm').onsubmit=async e=>{e.preventDefault();const hostname=new FormData(e.currentTarget).get('hostname').trim();try{const d=await api(`/api/workspaces/${state.workspace}/domains`,{method:'POST',body:JSON.stringify({hostname})});DQ('#domainCreatePanel').innerHTML=`<section class="domainCreator"><div class="composerTop"><div><span class="eyebrow">DNS VERIFICATION</span><h2>${DE(hostname)}</h2></div></div>${dnsCard(d.dns_record)}<div class="resourceActions"><button class="primary" onclick="verifyDomainHardening(${Number(d.domain?.ID??d.domain?.id)})">我已添加，立即验证</button><button onclick="document.querySelector('#domainCreatePanel').innerHTML=''">稍后处理</button></div></section>`;await load()}catch(err){DQ('#domainCreatorError').innerHTML=`<div class="productError">${DE(err.message)}</div>`}}};
window.rotateDomainHardening=async id=>{try{const d=await api(`/api/workspaces/${state.workspace}/domains/${id}/verify?rotate=1`,{method:'POST',body:'{}'});DQ('#domainCreatePanel').innerHTML=`<section class="domainCreator"><div class="composerTop"><div><span class="eyebrow">NEW DNS RECORD</span><h2>${DE(d.domain?.Hostname??d.domain?.hostname)}</h2></div><button onclick="document.querySelector('#domainCreatePanel').innerHTML=''">关闭</button></div>${dnsCard(d.dns_record)}<div class="resourceActions"><button class="primary" onclick="verifyDomainHardening(${id})">立即验证</button></div></section>`;await load()}catch(e){alert(e.message)}};
window.verifyDomainHardening=async id=>{try{await api(`/api/workspaces/${state.workspace}/domains/${id}/verify`,{method:'POST',body:'{}'});await load();const x=domainsCache.find(v=>Number(v.ID??v.id)===Number(id));if((x?.Status??x?.status)==='active'&&(x?.HTTPSStatus??x?.https_status)==='active')alert('DNS 与 HTTPS 验证均已通过，现在可以在短链接创建器中使用这个域名。')}catch(e){await load();alert(e.message)}};
window.GoJetProductHardening=window.GoJetProductHardening||{};window.GoJetProductHardening.domains=render;
})();
