(()=>{const header=document.querySelector('.gj-website-header');const updateSticky=()=>{if(header){if(window.scrollY>=80)header.setAttribute('data-sticky','true');else header.removeAttribute('data-sticky')}};updateSticky();addEventListener('scroll',updateSticky,{passive:true});const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const items=[...document.querySelectorAll('.reveal')];if(reduced||!('IntersectionObserver'in window)){items.forEach(el=>el.classList.add('is-visible'));return}const observer=new IntersectionObserver(entries=>{for(const entry of entries)if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}},{threshold:.12});items.forEach(el=>observer.observe(el));})();
